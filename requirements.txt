# Pinned versions verified working as of 2026-05-10
# See plan.md §11 for why each is pinned

# Core training stack
trl==1.4.0
transformers>=4.50.0,<4.60.0
accelerate>=1.3.0
peft>=0.14.0
datasets>=3.2.0
bitsandbytes>=0.45.0

# Unsloth — third-party perf wrapper for free-tier GRPO. HF-blessed but not first-party.
# See report.md §asterisks. Falls back to raw TRL if Unsloth has model-specific issues.
unsloth==2026.5.5  # adjust to current beta tag if newer is available

# vLLM — for GRPO rollout colocate
vllm>=0.6.0,<0.8.0

# Reward function deps
networkx>=3.2  # graph_edit_distance for R_GED
nltk>=3.8      # Google-BLEU for eval

# Cypher execution backends
kuzu==0.11.3            # primary; archived but functional
# falkordblite>=0.10.0  # OPTIONAL; requires Python ≥ 3.12; uncomment if Kaggle env allows

# HF Hub
huggingface_hub>=0.27.0

# Optional: experiment tracking
# wandb>=0.18.0
